# Security policy
The provider opens the store read-only, makes no network calls and writes nothing. Evidence strings are built from route paths, repository names and call sites in the store and are shown to the agent. Report vulnerabilities to nikhil@wirevow.com or via a private GitHub security advisory; use synthetic data in reproductions. The latest minor release receives fixes.
